function PathFinder(_terrain) {

  function Node() {
  
  }

  function queryPath(start, end) {
    const 

    //Once we have 2 valid points, do an A*

    //Returns a list of cell
  }
}

// function NavigationSystem(pathFinder, mouse) {

//   function update(actors) {
//     //Get the position of the mouse
//     //Bring it back inside the map

//     //For each actor
//       //Calculate a path
//       //Take 2 cells ahead
      
//     const component = actor.getComponent(ActorComponentId.NAVIGATION)
//     if (component === undefined) return
//   }
  
//   return {
//     update
//   }
// }

// const actor = Actor(0)
// const terrain = null
// const pathFinder = PathFinder(terrain)
// const start = Vector2D.ZERO
// const end = Vector2D.ZERO
// const mouse = null
// const navigationSystem = NavigationSystem(pathFinder, mouse)
// pathFinder.queryPath(start, end)